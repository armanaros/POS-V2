// deliveryLinks utility removed Lalamove support per request.
// Keep a minimal stub to avoid import errors from older code.

export function getDeliveryLink() {
  return '';
}

export default getDeliveryLink;
